@model backend.Models.Battery

@{
    ViewData["Title"] = "Редактировать аккумулятор";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8">
            <h1>@ViewData["Title"]</h1>
            <hr />

            <form asp-action="Edit" method="post" class="needs-validation" novalidate>
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                <input type="hidden" asp-for="Id" />

                <div class="row mb-3">
                    <div class="col-md-6">
                        <label asp-for="Article" class="form-label">Артикул</label>
                        <input asp-for="Article" class="form-control" />
                        <span asp-validation-for="Article" class="invalid-feedback"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Name" class="form-label">Название</label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="invalid-feedback"></span>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-4">
                        <label asp-for="Price" class="form-label">Цена (руб.)</label>
                        <input asp-for="Price" type="number" step="0.01" class="form-control" />
                        <span asp-validation-for="Price" class="invalid-feedback"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="Quantity" class="form-label">Количество</label>
                        <input asp-for="Quantity" type="number" class="form-control" />
                        <span asp-validation-for="Quantity" class="invalid-feedback"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="VoltageV" class="form-label">Напряжение (V)</label>
                        <input asp-for="VoltageV" type="number" class="form-control" />
                        <span asp-validation-for="VoltageV" class="invalid-feedback"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="CapacityAh" class="form-label">Емкость (Ач)</label>
                    <input asp-for="CapacityAh" type="number" class="form-control" />
                    <span asp-validation-for="CapacityAh" class="invalid-feedback"></span>
                </div>

                <div class="mb-3">
                    <button type="submit" class="btn btn-success">Сохранить изменения</button>
                    <a asp-action="Index" class="btn btn-secondary">Отмена</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
